FROM node:22-slim
WORKDIR /app

RUN npm i -g pnpm

COPY package.json pnpm-workspace.yaml* tsconfig.base.json tsconfig.json ./
COPY libs ./libs
COPY apps ./apps

RUN pnpm install --ignore-scripts && pnpm -r run build

ENV PORT=3010
EXPOSE 3010

CMD ["node", "apps/infra-agent-service/dist/apps/infra-agent-service/src/main.js"]
