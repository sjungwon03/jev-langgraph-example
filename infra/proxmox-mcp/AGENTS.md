# Contributor guardrails

Work from an issue-numbered branch based on `dev`, open PRs to `dev`, and promote only reviewed `dev` into `main`. Releases are GitHub Release archives only; do not publish to npm or make live Proxmox calls in development or CI.
